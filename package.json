{
  "name": "jeu-test-aline",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Prototype web original d'action-RPG fantasy, preview GitHub Pages avant migration Unreal.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "serve": "node scripts/serve.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "npm run test && npm run build"
  },
  "engines": {
    "node": ">=20"
  }
}
